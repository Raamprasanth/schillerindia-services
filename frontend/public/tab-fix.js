// tab-fix.js — Standard Native Tab navigation.
// Restores default native browser behaviors: tabbing into date fields does not force open the picker,
// and date entries can be typed smoothly with the keyboard without any auto-advancing interruptions.
(function () {
  // Let the browser handle standard input tabbing and picking natively.
})();
