{
  "name": "schillerindia-services-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check:html": "node scripts/check-html-scripts.js",
    "check:reports": "node scripts/check-report-page.js",
    "install:backend": "npm install --prefix backend",
    "install:python": "python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r requirements.txt",
    "postinstall": "npm run install:backend && npm run install:python",
    "build": "npm run install:backend && npm run install:python",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.4",
    "nodemailer": "^6.9.16"
  },
  "engines": {
    "node": ">=18"
  }
}
